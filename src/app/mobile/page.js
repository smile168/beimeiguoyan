import { getCigarettes } from '../lib/Cigarettes';

export default async function MobileHome() {
  // const data = await getCigarettes();
  // const id = data.id;
  // return <p>mobile home, with mongo {id} restart</p>;
  return <p>hellp from mobile page</p>;
}
